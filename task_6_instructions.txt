Implement Task 6 of the Inky Next Implementation Plan.

Worktree directory: /home/adam/Documents/Dev/inky/.worktrees/tauri-migration
Project directory: /home/adam/Documents/Dev/inky/.worktrees/tauri-migration/inky-next

1. Create inky-next/src/lib/components/Preview.svelte with the following content:
<script>
  import { storyHistory } from '$lib/stores';
  import { afterUpdate } from 'svelte';

  let scrollContainer;

  afterUpdate(() => {
    if (scrollContainer) {
      scrollContainer.scrollTo(0, scrollContainer.scrollHeight);
    }
  });
</script>

<div bind:this={scrollContainer} class="h-full overflow-y-auto p-4 bg-slate-900 text-slate-200">
  {#each $storyHistory as item}
    <div class="mb-4">
      {#if item.type === 'text'}
        <p>{item.content}</p>
      {:else if item.type === 'choice'}
        <button class="block w-full text-left p-2 border border-slate-700 hover:bg-slate-800 rounded">
          {item.content}
        </button>
      {/if}
    </div>
  {/each}
</div>

2. Verify the file exists and contains the code.
3. Commit the change with message: "feat: implement preview ui"
